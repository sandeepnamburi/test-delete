import React from "react";
import Logo from "../src/logo.png";
const Nav = () => {
  return (
    <nav>
      <img className = "img" src={Logo} alt="logo" />
    </nav>
  );
};

export default Nav;
